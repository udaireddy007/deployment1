{
        "cmd": ["php", "$file"]
}

save it with PHP.submile-build




{
  "cmd": ["groovy", "$file"],
  "selector": "source.groovy",
  "file_regex": "[ ]*at .+[(](.+):([0-9]+)[)]",

  "windows": {
    "shell": "cmd.exe"
  }
}
